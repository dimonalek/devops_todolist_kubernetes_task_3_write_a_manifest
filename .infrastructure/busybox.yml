apiVersion: v1
kind: Pod
metadata:
  name: busybox-curl
  namespace: todoapp
spec:
  containers:
    - name: busybox-curl
      image: ikulyk404/busyboxplus:curl
      command: ["sleep", "3600"]
  restartPolicy: Always
